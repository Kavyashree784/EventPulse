spring.datasource.url=jdbc:mysql://localhost:3306/eventpulse?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.thymeleaf.cache=false
server.port=8081

# WebSocket message broker settings (optional tuning)
# spring.websocket.message-broker.enabled=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ghoraav.kc2022@vitstudent.ac.in
spring.mail.password=vryr qvxj qxjk aera
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.debug=true

# Provide an explicit Hibernate dialect to avoid "Unable to determine Dialect without JDBC metadata" when
# the database is not reachable or metadata cannot be read. Replace with your DB dialect if different.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
